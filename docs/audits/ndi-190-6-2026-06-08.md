# NDI Compliance (#6) — DSHEA §8 / 21 U.S.C. 350b / 21 CFR 190.6 Registry + Audit

**Date:** 2026-06-08 · **Auditor:** CC (authority-anchor §8 step 3; artifact #6).

---

## Citations (foundational — confirmed)
- **DSHEA §8 / 21 U.S.C. 350b** — NDI definition + the pre-**October 15, 1994** grandfather date.
- **21 CFR 190.6** — the 75-day pre-market NDI notification.
- **DSHEA §201(ff)(3)(B)** — the exclusion FDA invokes for NMN / NAD+ (cited in those entries).
- All cited in code; the date + sections are foundational and correct.

## Audit result — NDI wiring CONFIRMED sound (the pattern holds, 5th surface)
`lib/supplementNDI.ts`:
- **5-state classification** — grandfathered / notified / gras-food / required / unknown, each with advisory text + (where applicable) NDI number.
- **Word-boundary matching** (Wave 1.5d) — refactored from substring (`includes`) after the choline overgeneralization bug; `KEYWORD_REGEX_CACHE` + per-keyword `boundaryMode`.
- **Safe default** — unmatched → `unknown` + "verify compliance status independently" (correct UNDOCUMENTED UX, per the two-state NDI discipline).
- **`hasRisk`** gate — true when any `required` ingredient present.

## Per-ingredient overrides (the B6-analog) — harnessed
- **Choline `standalone-token`** — `Choline Bitartrate` → grandfathered (parent ODI); `Phosphatidylcholine` / Alpha-GPC / CDP-Choline do **NOT** inherit it (compound forms have distinct branded NDIs). The structural-moiety boundary override.
- **Vitamin-D source-independence** — `Vitamin D3 Vegan (Lichen-Sourced)` → grandfathered. A different botanical/animal SOURCE is not a chemical alteration, so lichen/algal D3 inherits the pre-1994 ODI status (explicit `vitamin d3`/`vitamin d2` keywords because the `\b` boundary won't let bare "vitamin d" match "Vitamin D3").

## High-risk `required` flags — spot-checked correct
NAC (FDA drug-status posture), NMN + NAD+ (DSHEA §201(ff)(3)(B) exclusion), racetams (unapproved drugs), sulbutiamine (synthetic B1 analog) — all correctly flagged as the high-risk tier. These are the genuinely contested ingredients; the table's posture matches current FDA enforcement.

## Verification note (threat-model calibration)
NDI status is a **regulatory determination** per ingredient (no single authoritative table — it's pre-1994 market history + the FDA NDI database). The foundational citations are confirmed and the **high-stakes `required` classifications are spot-checked correct**; exhaustive per-ingredient NDI-database verification is **PA-review-tier** (queued, not blocking — `docs/pa-verification/`). Per [[feedback_regulatory_classification_vs_supplier_data]], NDI classification doesn't vary by supplier.

## Harness (spec §5)
`harness-ndi-190-6-golden.test.ts` (8): the 5-state verdict + `hasRisk`; the choline standalone-token override (parent grandfathered, compound unknown); the lichen-D3 source-independence; the unknown safe-default. Existing `supplement-ndi-keyword-discipline.test.ts` covers the boundary-mode discipline.
