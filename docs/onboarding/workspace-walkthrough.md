# Workspace Walkthrough / Onboarding — scaffold (2026-06-22)

**Status:** scaffold. **Mirrors the canonical operator journey** ([pre-launch-checklist §A](../launch/pre-launch-checklist.md)) — onboarding walks the *same path* we ship, test, and demo (one path, drift-proof; same shape as the export chokepoint). Built as **in-product onboarding first**; the demo-video script is then a *recording of the actual in-product experience*, not a separate asset that decays.

**PENDING Wizard:** voice / tone / copy is operator-domain (the joy-of-mastery brand voice). CC scaffolds the **steps** + which surface each introduces + the teaching beat; **Wizard writes the words.**

## Walkthrough steps (mirror §A — auto-load the canonical "Sleep & Recovery Stack" as the sample start)

| # | Surface introduced | Teaching beat (copy PENDING Wizard) |
|---|---|---|
| 1 | **Bulk Paste** | "Start from a real formula." Auto-load the canonical sample instead of an empty workspace. |
| 2 | **Resolution rows** | Tier-1 matches (green) **and** the Tier-4 **UNDOCUMENTED** marker — *the engine tells you what it can't verify.* The honest-engine moment. |
| 3 | **Force-pick chooser** | "DHA" → the chooser; *why* it forces a pick (allergen + %DV depend on the form) — honest-not-silent-default. Fish → species cascade. |
| 4 | **Facts panel** | the PENDING-factor render — *honest %DV, never a guessed number.* |
| 5 | **Harm-critical export gate** | trigger the refusal (disease-claim or allergen) — *the engine won't let you produce a non-compliant label.* |
| 6 | **Formulation Verification Report** | where the FVR export sits + what it's for (hand to your regulatory reviewer); catalog-verified vs operator-attested. |

**Surface order:** Bulk Paste → resolution rows → force-pick chooser → analysis tabs (Facts / safety / NDI / allergen) → export gate → FVR.

## Demo-video script source

This walkthrough, narrated in brand voice (PENDING Wizard), **is** the demo script — recorded against the live product so it can't drift from what ships. The canonical journey does triple duty: onboarding tour, launch smoke-test, demo.

---

*Next session: Wizard authors the copy/voice; decide in-product mechanism (tooltips / guided tour / sample auto-load); co-founder ratifies the sample formulation (shared with the test library).*
