# CC Session Handoff — 2026-06-04

**For the next CC: read this first, then the linked memories. Verify ground state before executing (git state below may have moved).**

---

## Where we are (ground truth at handoff)

- **Branch:** `main`, **ahead of `origin/main` by 4 commits** (PUSH HELD — do not push without operator say-so).
  - `5f56691` docs: production-execution roadmap
  - `8e36a38` Redesign Phase 1: Home KPI stats in mono
  - `f59ef41` Redesign Phase 1: status-card severity hierarchy + grid fix
  - `87b53b4` Redesign Phase 1: global type + atmosphere + Home hero
- **Working tree:** clean except untracked `app/preview/` (throwaway design reference — `/preview` route, DO NOT SHIP) and `docs/catalog/` (pre-existing, not this session's).
- **Everything is committed or on disk. A VS restart loses nothing.**

## What shipped this session

1. **WS-A — auth + cloud save (launch-blocker #4): COMPLETE, verified live in prod, PUSHED.** Per-user auth (`/auth`), Supabase round-trip (`lib/cloudSync.ts`), invite-only whitelist, localStorage→cloud migration on sign-in. See [[ws-a-save-auth-complete-2026-06-02]].
2. **Redesign Phase 1 — "Arcane Instrument": committed, PUSH HELD.** Geist font, canvas grid, gold-serif Home hero, status-card severity hierarchy, mono dashboard stats. Dark = "Synthwave Lab." Extends the EXISTING `globals.css` token system. See [[redesign-phase-1-2026-06-04]].
3. **LB#3 unit fix** — supplements default ingredient unit → `mg` (`defaultIngredientUnit()` in `lib/modes.ts`).
4. **Production-execution roadmap** (operator brain dump) banked → `docs/roadmap/production-execution-roadmap-2026-06-04.md`.

## ⛔ THE NEXT ACTION — WS-C multi-user (committed August deliverable)

**OPEN DECISION, waiting on operator:** confirm the **sharing model** = *workspace/org* (brand owner owns the workspace, invites CMO + RA as members, **roles** govern visibility). The operator was about to confirm when the session hit an API error.

Once confirmed, the build chain is:
1. Sharing schema (design → operator/Opus review BEFORE touching prod)
2. ⛔ **Confidentiality hardening + RLS negative tests** — load-bearing milestone (leaks = ends the company). This is the one that must be airtight.
3. Roles / visibility
4. Invite flow (email already wired via Google Workspace)
5. Version-lock

Recommend an **Opus architecture pass** on the schema before code (relay rhythm).

## Operator-pending (not blocking local WS-C dev)

- Set Vercel env vars `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` + redeploy → activates auth/save on the **live** site.
- Confirm the WS-C sharing model (above).

## Guardrails — do not violate

- **Regulated outputs stay byte-faithful FDA-standard** (Supplement/Nutrition Facts, label, FDA docs) — redesign treats CHROME only. [[regulated-outputs-stay-fda-standard]]
- **Don't let redesign Phase 2 / the brain-dump roadmap eat WS-C** — WS-C is the committed deliverable; everything else is banked/post-launch.
- **Push held** — commit locally; don't push without operator say-so. [[never-infer-green-light]]
- **Run vitest via PowerShell**, not Bash (Bash flakes the worker spawn). [[run-vitest-via-powershell]]
- **Verify ground state at session start.** [[verify-ground-state-at-start-of-autonomous-session]]

## Near-term-small / optional (do NOT displace WS-C)

- Verify the A/M-ratio / acetic-acid math (safety-relevant, cheap; gates acidified-foods 21 CFR 114).
- "Build X" tab rename (IA tidy).
- Master Specs un-gating — give it Supabase persistence (WS-A just unblocked this), then flip `MASTER_SPECS_FEATURE_FLAG`.

## Read-first memories

`ws-a-save-auth-complete-2026-06-02` · `redesign-phase-1-2026-06-04` · `regulated-outputs-stay-fda-standard` · `production-execution-roadmap-2026-06-04` · `path-to-august-scoped-launch-2026-06-01`
