# Shell scripts must stay LF — bash on Windows/WSL fails silently on CRLF.
# Critical for .claude/hooks/*.sh per Catalog Authoring Rulebook §VI.29
# automation (pre-commit gate hook).
*.sh text eol=lf
